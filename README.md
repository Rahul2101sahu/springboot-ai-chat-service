# springboot-ai-chat-service
The AI Chat Service is a Spring Boot–based backend application that simulates an AI-powered chat system. The service allows users to send messages through a REST API, forwards the request to a mocked Large Language Model (LLM) service to generate a response, stores the conversation history in a database, and returns the AI-generated reply.
